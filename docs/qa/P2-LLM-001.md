# Evaluator QA Report

Feature ID: P2-LLM-001

Date: 2026-06-06

Evaluator: generator agent

## 复现步骤

1. 运行 `& 'C:\nvm4w\nodejs\pnpm.cmd' --filter @story2script/server test`。
2. 运行 `& 'C:\nvm4w\nodejs\pnpm.cmd' typecheck`。
3. 运行 `& 'C:\nvm4w\nodejs\pnpm.cmd' lint`。
4. 运行 `& 'C:\nvm4w\nodejs\pnpm.cmd' verify`。
5. 运行 `& 'C:\nvm4w\nodejs\pnpm.cmd' test:ui`。
6. 观察 server Vitest 中真实 Express app 的 `/api/screenplay/generate` 请求。
7. 确认测试里的本地 OpenAI-compatible server 收到 `/responses` 请求，并返回 YAML。
8. 确认 Playwright evaluator 在浏览器里编辑小说输入，拦截 `/api/screenplay/generate`，并断言请求体包含完整 `novel`。

## 截图或日志证据

- Screenshot: 不适用，本轮无 UI 结构变更。
- Trace: 不适用。
- Server logs:
  - mock 路径输出 `screenplay.generate.completed`，`provider:"mock"`，HTTP 200。
  - openai 路径输出 `screenplay.generate.completed`，`provider:"openai"`，HTTP 200。
  - 缺 key 路径输出 `screenplay.generate.provider_failed`，HTTP 502。
- Command output:

```text
& 'C:\nvm4w\nodejs\pnpm.cmd' --filter @story2script/server test: PASS
server test files: 3 passed
server tests: 17 passed

& 'C:\nvm4w\nodejs\pnpm.cmd' typecheck: PASS
shared/server/web typecheck passed

& 'C:\nvm4w\nodejs\pnpm.cmd' lint: PASS
eslint . passed

& 'C:\nvm4w\nodejs\pnpm.cmd' verify: PASS
typecheck: passed
lint: passed
test: shared 1 passed, server 17 passed, web passWithNoTests passed
build: shared/server/web passed

& 'C:\nvm4w\nodejs\pnpm.cmd' test:ui: PASS
Playwright Chromium: 2 passed
- smoke: home page displays mock YAML through the backend
- evaluator: sends edited novel text to screenplay generate API
```

## 覆盖范围

- OpenAI-compatible 成功路径：本地 fake `/responses` 返回 fenced YAML，provider 剥离 code fence 后返回给 route。
- 即时校验：成功 YAML 返回 `validation.valid:true`。
- 精确错误路径：provider 返回缺少 `project.title` 的 YAML，route 返回 `validation.valid:false` 和 `project.title` 错误。
- 请求边界：openai 模式缺小说文本返回 `400 BAD_REQUEST`。
- 配置边界：openai 模式缺 `OPENAI_API_KEY` 返回 `502 LLM_UNAVAILABLE`，不暴露密钥。
- mock 回归：默认 mock 路径仍返回 `examples/screenplay-sample.yaml`，并通过完整 validator。
- 前端接线：现有 textarea 内容会作为 `novel` 传入 generate API，mock 仍保持原行为，并通过 Playwright Chromium smoke。
- Evaluator 覆盖：`apps/web/tests/ui/p2-evaluator.spec.ts` 在浏览器中编辑 textarea，拦截 generate API，请求体断言 `novel` 等于编辑后的全文，再返回 evaluator fixture YAML 给页面渲染。

## 外部 API 说明

本轮没有调用真实外部 OpenAI API，也没有创建或提交 `.env`。原因是自动化验收不应依赖真实密钥、余额、限流或网络可用性。兼容性通过本地 HTTP server 验证：它接收真实 provider 发出的 `POST /responses`，并按 OpenAI-compatible Responses 响应形状返回 `output[].content[].text`。

## 子代理 evaluator 复核

Evaluator sub-agent: `019e9bc2-4016-73c3-954d-062cd3a0b435`

子代理未修改代码，只做独立验证。执行命令：

```powershell
& 'C:\nvm4w\nodejs\pnpm.cmd' test:ui
& 'C:\nvm4w\nodejs\pnpm.cmd' --filter @story2script/server test
& 'C:\nvm4w\nodejs\pnpm.cmd' verify
```

复核结果：全部 PASS。

子代理确认：

- `apps/web/tests/ui/p2-evaluator.spec.ts` 在真实浏览器中打开页面、编辑 `textarea`、点击生成，并断言 `request.postDataJSON()` 包含 `{ novel: evaluatorNovel }`，能证明前端把编辑后的小说全文作为 `novel` 发给 `/api/screenplay/generate`。
- `apps/server/tests/screenplay-route.test.ts` 覆盖 mock 回归、OpenAI-compatible 成功路径、fenced YAML 剥离后即时校验、缺 `project.title` 的 validation 错误、openai 模式缺输入 400、缺 key 502。
- 非阻塞观察：前端 evaluator 使用 `toMatchObject({ novel: evaluatorNovel })`，锁住了本轮关键字段，但没有要求 request body 完全等于某个对象；这对 `P2-LLM-001` 当前目标足够。

子代理结论：无阻塞问题，建议放行。

## 评分

| 维度 | 分数 | 说明 |
| --- | --- | --- |
| 用户路径完整度 | 5/5 | openai 模式可以用小说文本走 `/api/screenplay/generate`，并返回 YAML + validation。 |
| 数据契约一致性 | 5/5 | 输出继续复用现有 v1.0 validator，bad YAML 会给精确路径。 |
| 错误与边界处理 | 5/5 | 覆盖缺小说文本、缺 key、mock 回归和 fenced YAML。 |
| UI 可用性 | 4/5 | 只做最小接线，已有输入会传给后端；未重做 mock-first 文案，留给后续 UI polish。 |
| 可维护性 | 5/5 | Provider 网络逻辑集中在 `OpenAIProvider`，route 只负责 prompt、错误映射和校验串联。 |

## 是否放行

Decision: PASS

## 修复建议

- Phase 3 可以继续实现 chapter split 与 multi-stage pipeline。
- Phase 4/5 再统一更新前端文案、loading、错误态和 demo 路线。
